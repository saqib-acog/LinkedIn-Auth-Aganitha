<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <title>LinkedIn Profile Card</title>
    <style>
      * {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
      }
      html {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
          sans-serif;
        background-color: black;
      }
      header {
        text-align: center;
        background-color: rgb(24, 24, 24);
        padding: 20px;
        color: white;
      }
      header .in-linkedin {
        color: rgb(44, 44, 255);
        padding: 3px;
        margin: 0 3px;
        border-radius: 5px;
        background-color: white;
      }
      main {
        min-height: calc(100vh - 200px);
        display: flex;
        justify-content: center;
        align-items: center;
      }
      main .card {
        background-color: rgb(20, 20, 20);
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 20px;
        border-radius: 5px;
        font-size: 25px;
        gap: 40px;
      }
      .card .card-data {
        display: flex;
        gap: 25px;
        flex-direction: column;
        color: antiquewhite;
      }
      .card img {
        border-radius: 50%;
        border: 5px solid white;
        width: 200px;
        aspect-ratio: 1/1;
      }
      .card-data p span {
        font-weight: 500;
        color: aqua;
      }
      .logout-btn {
        padding: 15px 40px;
        border-radius: 5px;
        text-align: center;
        background-color: rgb(194, 0, 0);
        color: white;
        text-decoration: none;
        border: none;
        font-size: 25px;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>Linked<span class="in-linkedin">in</span> Card</h1>
    </header>
    <main>
      <div class="card">
        <% if (user.picture) { %>
        <img src="<%= user.picture %>" alt="Profile Picture" />
        <% } else { %>
        <img
          src="https://t3.ftcdn.net/jpg/03/64/62/36/360_F_364623623_ERzQYfO4HHHyawYkJ16tREsizLyvcaeg.jpg"
          alt="Default Image"
        />
        <% } %>
        <div class="card-data">
          <p>LinkedIn Id : <span><%= user.sub %></span></p>
          <p>Name : <span><%= user.name %></span></p>
          <p>Email : <span><%= user.email %></span></p>
        </div>
        <form action="/logout?_method=DELETE" method="POST">
          <button class="logout-btn" type="submit">
            Logout <i class="fa fa-sign-out"></i>
          </button>
        </form>
      </div>
    </main>
  </body>
</html>
